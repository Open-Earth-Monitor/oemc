import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Distributor - Open-Earth-Monitor Cyberinfrastructure',
  description: '...',
};

const Distributor: React.FC = () => (
  <div>
    <h1>Distributor</h1>
  </div>
);

export default Distributor;
